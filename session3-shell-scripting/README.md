# Shell Scripting Assignment

## System Information Script

Created a Bash script to display system information and practice
variables, user input, directory/file creation, commands, and output
redirection.

### Script

`system-info.sh`

### Commands Used

- `read -p` - takes input from the user
- `mkdir` - creates a directory
- `touch` - creates a file
- `echo` - displays text
- `date` - displays the current date
- `hostname` - displays the system hostname
- `whoami` - displays the current username
- `df -h` - displays disk usage
- `ps` - displays running processes
- `>` - redirects command output to a file

### Output

![System Information Output](screenshots/system-info-output.png)

### Output Redirection

The running process information was redirected to a file using `>`.

![Output File](screenshots/system-info-file.png)
